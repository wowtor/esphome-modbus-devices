substitutions:
  hostname: omnia-adapter
  friendly_name: Omnia Adapter

# Configure the modbus controller with one or more modbus devices
modbus_controller:
  - id: omnia
    address: 1
    setup_priority: -10
    update_interval: 20s

packages:
  remote_packages:
    url: https://github.com/wowtor/esphome-modbus-devices
    ref: main
    refresh: 0s
    files:
      #- common/ethernet.yaml
      #- common/wifi-configured.yaml,
      #- common/wifi-unconfigured.yaml,
      #- path: ferroli-omnia/entities.yaml
      #  vars:
      #    controller_id: omnia
      #    prefix: Omnia
      #- path: modbus/ws100_kwh_meter.yaml
      #  vars:
      #    controller_id: omnia_meter
      #    prefix: Omnia Meter
      #- path: modbus/flair400.yaml
      #  vars:
      #    controller_id: flair
      #    prefix: Ventilation
      #- path: modbus/sdm120.yaml
      #    vars:
      #      controller_id: kookplaat
      #      prefix: Kookplaat
      #- path: modbus/ws100_kwh_meter.yaml
      #  vars:
      #    controller_id: pv_meter
      #    prefix: PV Meter

esphome:
  name: $hostname
  #name_add_mac_suffix: true

esp32:
  board: esp-wrover-kit
  framework:
    type: arduino

uart:
  tx_pin: 14
  rx_pin: 15
  baud_rate: 9600
  stop_bits: 1
  parity: NONE

modbus:

# Enable logging
logger:

# Enable Home Assistant API
api:
  password: ""

ota:
  password: ""
  platform: esphome

text_sensor:
  - platform: version
    hide_timestamp: true
    name: "${friendly_name} ESPHome Version"
    entity_category: diagnostic

preferences:
  # set write interval to maximum (stored as milliseconds in 32bit unsigned int)
  flash_write_interval: 4294967295 ms
